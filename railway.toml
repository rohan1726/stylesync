[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd server && npx prisma generate && npx prisma db push && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
